services:
  db:
    image: postgres
    restart: unless-stopped
    ports:
      - "5432:5432"
    volumes:
      - db-data:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: securaApi
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password

volumes:
  db-data:
